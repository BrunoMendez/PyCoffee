program patito;
var x,y,z, age: int; ch: char;

void function pelos(y: int, year: int)
var x: int;
{
    input(age);
    x = y;
    while (x < 10) {
        print(age - x);
        x = x + 1;
    }
}

main() {
    for(z=1:3) {
        x = 1+1;
    }
    x = 5 + 5 * 3 / 1;
    y = 4 * 3;
}